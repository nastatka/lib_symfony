<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Добавление книги{% endblock %}</title>
    </head>
    <body>
        {% block body %}
        <h1>{{title}}</h1>
        <div>{{form(form) }}</div>
        {% endblock %}
    </body>
</html>